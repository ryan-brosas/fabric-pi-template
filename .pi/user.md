# User Profile

## Identity

- Name: (not set)
- Role: Solo developer

## Communication

- Detail level: Detailed — full explanations and reasoning.

## Git Workflow

- Preference: Auto-commit — for Git target projects only.
- Note: This authoring workspace (`new-system`) is non-Git, and the host
  `pi.json` keeps `git commit *: ask`. Auto-commit applies only when
  working in a Git target project that opts into it; it does not override the
  host ask-first rule here.

## Orchestration

- Preference: make delegation visible. For every non-trivial multi-step coding task, invoke `/team` or spawn bounded Fabric agents and announce their roles before dispatch; do not silently complete substantive coding primary-only.
- Analysis-only questions and genuinely small, known edits may remain direct under the governing direct-first rule.

## Preferences Reference

This file is for on-demand reference — not injected via `instructions[]`. Use
`read .pi/user.md` when preferences are needed.
